{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-events",
  "description": "Allow the NoirSound Connect main window to subscribe to backend state events.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
