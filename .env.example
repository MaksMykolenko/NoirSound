# Real API mode (Requires backend to be running)
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=http://localhost:3000/api

# Mock API mode (Runs frontend purely with mock data, no backend required)
# VITE_USE_MOCK_API=true
